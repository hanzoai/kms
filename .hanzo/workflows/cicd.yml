# The whole pipeline is hanzoai/ci's build.yml reading hanzo.yml: the gate, the
# images and the console site.
name: CI/CD

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  cicd:
    uses: hanzoai/ci/.github/workflows/build.yml@v1
    secrets: inherit
